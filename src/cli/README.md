# CLI

This folder contains the Totistack command-line interface.

## Structure
- `commands/`: command handlers
- `prompts/`: prompt schemas for interactive flows
- `services/`: orchestration and persistence services
- `shared/`: internal CLI utilities
