{
  "name": "totistack-functions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "src/index.js",
  "scripts": {
    "check": "node --check src/index.js && find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {}
}
