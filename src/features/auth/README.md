# Auth feature

Firebase authentication starter for Totistack.

## Design

- Contributes routes, collections, services, and starter UI only.
- Runtime state lives in the root `appStore`.
- Works with the shared shard-provider instance.
- Can run with RBAC enabled or disabled.
