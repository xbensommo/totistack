<template>
  <div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">
    <article v-for="item in items" :key="item.label" class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
      <p class="text-xs font-medium uppercase tracking-[0.22em] text-slate-500">{{ item.label }}</p>
      <p class="mt-3 text-3xl font-semibold tracking-tight text-slate-900">{{ item.value }}</p>
      <p v-if="item.hint" class="mt-2 text-sm text-slate-500">{{ item.hint }}</p>
    </article>
  </div>
</template>

<script setup>
/**
 * @file components/MetricsGrid.vue
 * @description Generic metrics grid for feature overview pages.
 */
defineProps({
  items: { type: Array, default: () => [] },
})
</script>
