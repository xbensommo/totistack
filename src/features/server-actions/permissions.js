/** @file src/features/server-actions/permissions.js */

export const SERVER_ACTIONS_PERMISSIONS = Object.freeze({
  VIEW: 'serverActions.view',
  MANAGE: 'serverActions.manage',
  RUN: 'serverActions.run',
})

export default SERVER_ACTIONS_PERMISSIONS
